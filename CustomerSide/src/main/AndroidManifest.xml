<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.manddprojectconsulant.greedapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Greed">
        <activity android:name=".Activities.AdminDashboardActivity"></activity>
        <activity android:name=".Activities.DetailImageActivity" />
        <activity android:name=".Activities.FestivalActivity.JanmashtamiGalleryActivity" />
        <activity android:name=".Activities.FestivalActivity.ChristmasGalleryActivity" />
        <activity android:name=".Activities.FestivalActivity.HoliGalleryActivity" />
        <activity android:name=".Activities.FestivalActivity.DiwaliGalleryActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.RegistrationActivity" />
        <activity android:name=".Activities.DashboardActivity" />
        <activity
            android:name=".Activities.SplashActivity"
            android:configChanges="orientation|keyboardHidden"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>